#nullable enable
namespace JiraTools.Configuration
{
    /// <summary>
    /// Enumeration of possible project statuses
    /// </summary>
    public enum ProjectStatus
    {
        ToDo,
        InProgress,
        Review,
        Done,
        Blocked
    }
}
